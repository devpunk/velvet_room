import Foundation

extension MSfo
{
    //MARK: internal
    
    static func factoryValue(
        item:MSfoItem,
        header:MSfoHeader,
        data:Data) -> MSfoValueProtocol?
    {
        
    }
}
